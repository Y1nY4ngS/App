# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/
jspm_packages/

# Build and dist folders (commonly used in React, Next.js, or other build setups)
build/
dist/
out/
.next/

# Coverage directory used by testing tools (e.g. Jest, Istanbul)
coverage/

# Optional cache folders (e.g. parcel, babel)
.cache/
.cache-loader/
.eslintcache
*.tsbuildinfo

# OS-specific files
.DS_Store
Thumbs.db

# Environment variables (keep secrets out of version control!)
.env
.env.*  # e.g. .env.local, .env.production

# If you use TypeScript, ignore declaration files if you prefer
*.d.ts

# Lock files (optional, depending on your workflow)
# package-lock.json
# yarn.lock

# Other files you don't want in version control can be added here
